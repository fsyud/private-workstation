import React from "react";
import "@pages/options/Options.css";

const Options: React.FC = () => {
  return <div className="OptionsContainer">
    Options can config some options
  </div>;
};

export default Options;
